---
import { Image } from "astro:assets";
import MainLayout from "../layouts/MainLayout.astro";
import DeveloperDeck from "../components/DeveloperDeck.astro";
import McDonaldsIcon from "../components/McDonaldsIcon.astro";
import Logo from "../components/Logo.astro";
import GithubContributions from "../components/GithubContributions.astro";

import morataLandingImage from "../assets/projects/fmmcbs/morata_landing.png";
import fmmCbsLandingImage from "../assets/projects/fmmcbs/fmm_cbs_landing.png";
import emedicardLandingImage from "../assets/projects/emedicard/landing-page.png";
import emedicardAdminReviewImage from "../assets/projects/emedicard/admin-review.png?url";
import emedicardAdminReview2Image from "../assets/projects/emedicard/admin-review2.png?url";
import emedicardAdminApplicationListImage from "../assets/projects/emedicard/admin-application-list.png?url";
import emedicardCardTypeImage from "../assets/projects/emedicard/card-type.png?url";
import emedicardDocumentRequirementsImage from "../assets/projects/emedicard/document-requirements.png?url";
import emedicardHealthCardImage from "../assets/projects/emedicard/health-card.png?url";
import emedicardPaymentMethodsImage from "../assets/projects/emedicard/payment-methods.png?url";
import emedicardReviewSubmitImage from "../assets/projects/emedicard/review-submit.png?url";
import emedicardCertificateImage from "../assets/projects/emedicard/sample-health-certificate.svg?url";
import emedicardUploadDocumentsImage from "../assets/projects/emedicard/upload-documents.png?url";
import emedicardInspectorScannerImage from "../assets/projects/emedicard/inspector-qr-scanner.png?url";
import readoramaPreviewImage from "../assets/projects/readorama/readorama_purpose.png";
import readoramaBookSellingImage from "../assets/projects/readorama/readorama-book-selling.png?url";
import readoramaDesignSystemImage from "../assets/projects/readorama/readorama_preview.png?url";
import figmaIcon from "../assets/tech-icons/figma.svg?url";
import reactIcon from "../assets/tech-icons/react.svg?url";
import typescriptIcon from "../assets/tech-icons/typescript.svg?url";
import htmlIcon from "../assets/tech-icons/html.svg?url";
import vueIcon from "../assets/tech-icons/vue.svg?url";
import tailwindIcon from "../assets/tech-icons/tailwindcss.svg?url";
import nextIcon from "../assets/tech-icons/nextdotjs.svg?url";
import nuxtIcon from "../assets/tech-icons/nuxt.svg?url";
import astroIcon from "../assets/tech-icons/astro.svg?url";
import laravelIcon from "../assets/tech-icons/laravel.svg?url";
import phpIcon from "../assets/tech-icons/php.svg?url";
import pythonIcon from "../assets/tech-icons/python.svg?url";
import fastapiIcon from "../assets/tech-icons/fastapi.svg?url";
import nodeIcon from "../assets/tech-icons/nodedotjs.svg?url";
import postgresqlIcon from "../assets/tech-icons/postgresql.svg?url";
import convexIcon from "../assets/tech-icons/convex.svg?url";
import mysqlIcon from "../assets/tech-icons/mysql.svg?url";
import clerkIcon from "../assets/tech-icons/clerk.svg?url";
import dockerIcon from "../assets/tech-icons/docker.svg?url";
import cloudflareIcon from "../assets/tech-icons/cloudflare.svg?url";
import amazonS3Icon from "../assets/tech-icons/amazon-s3.svg?url";
import vercelIcon from "../assets/tech-icons/vercel.svg?url";
import railwayIcon from "../assets/tech-icons/railway.svg?url";
import expoIcon from "../assets/tech-icons/expo.svg?url";
import codexIcon from "../assets/tech-icons/codex.svg?url";
import claudeIcon from "../assets/tech-icons/claude.svg?url";
import antigravityIcon from "../assets/tech-icons/antigravity.svg?url";
import {
  ArrowUpRight,
  ChevronLeft,
  ChevronRight,
  Database,
  ExternalLink,
  FileText,
  Layers,
  Mail,
  MapPin,
  Maximize2,
  Smartphone,
  Terminal,
  User,
  Briefcase,
  Bot,
  GraduationCap,
  Code,
  Globe,
  Lock,
  Menu,
  Moon,
  Sun,
  X,
} from "@lucide/astro";

export const prerender = true;

const cvHref = "/docs/cv.pdf";

const navItems = ["Projects", "Skills", "Experience", "Contact"];

const profileLinks = [
  {
    id: "link-github",
    label: "GitHub",
    href: "https://github.com/SeanPqul",
    icon: ExternalLink,
  },
  {
    id: "link-linkedin",
    label: "LinkedIn",
    href: "https://www.linkedin.com/in/sean-paul-lapasanda-219117241/",
    icon: ExternalLink,
  },
  {
    id: "link-email",
    label: "Email",
    href: "mailto:seanpaullapasanda@gmail.com",
    icon: Mail,
  },
];

const skills = [
  {
    title: "Frontend",
    icon: Layers,
    items: ["React", "TypeScript", "HTML", "Tailwind CSS", "Next.js", "Astro", "Figma", "Nuxt.js", "Vue.js"],
  },
  {
    title: "Backend",
    icon: Terminal,
    items: ["Laravel", "PHP", "Python", "FastAPI", "Node.js", "TypeScript", "PostgreSQL", "MySQL", "Convex DB", "Clerk"],
  },
  {
    title: "Cloud & DevOps",
    icon: Database,
    items: ["Docker", "Cloudflare", "Amazon S3", "Vercel", "Railway"],
  },
  {
    title: "Mobile",
    icon: Smartphone,
    items: ["React Native", "Expo", "Expo Router"],
  },
  {
    title: "AI Agents",
    icon: Bot,
    items: ["Codex", "Claude Code", "Antigravity"],
  },
];

const techIcons: Record<string, string> = {
  "React": reactIcon,
  "React Native": reactIcon,
  "TypeScript": typescriptIcon,
  "HTML": htmlIcon,
  "Tailwind CSS": tailwindIcon,
  "Next.js": nextIcon,
  "Nuxt.js": nuxtIcon,
  "Vue.js": vueIcon,
  "Astro": astroIcon,
  "Figma": figmaIcon,
  "Laravel": laravelIcon,
  "PHP": phpIcon,
  "Python": pythonIcon,
  "FastAPI": fastapiIcon,
  "Fast API": fastapiIcon,
  "Node.js": nodeIcon,
  "PostgreSQL": postgresqlIcon,
  "Convex DB": convexIcon,
  "MySQL": mysqlIcon,
  "Clerk": clerkIcon,
  "Docker": dockerIcon,
  "Cloudflare": cloudflareIcon,
  "Amazon S3": amazonS3Icon,
  "Vercel": vercelIcon,
  "Railway": railwayIcon,
  "Expo": expoIcon,
  "Expo Router": expoIcon,
  "Codex": codexIcon,
  "Claude Code": claudeIcon,
  "Antigravity": antigravityIcon,
};

const timelineItems = [
  {
    id: "ojt",
    type: "experience",
    role: "Software Developer Intern / OJT",
    company: "F.M. Morata Customs Brokerage & Law Firm",
    date: "Feb 2026 — May 2026",
    bullets: [
      "Built backend REST APIs using Laravel, implementing request validation and role access policies.",
      "Developed frontend features using React, TypeScript, and Vite, incorporating modular components and routing states.",
      "Improved document upload and transaction filtering workflows for brokerage operations."
    ],
    highlight: true,
    icon: Briefcase,
    accentColor: "text-mint",
    borderColor: "hover:border-mint/30",
    innerGlowColor: "group-hover:bg-mint/10",
    glowColor: "bg-mint/5",
    dotColor: "before:bg-mint",
    tag: "OJT Internship",
    techTags: ["Laravel", "React", "TypeScript", "MySQL"]
  },
  {
    id: "education",
    type: "education",
    role: "BS in Information Technology (BSIT)",
    company: "STI College Davao",
    date: "2022 — 2026",
    bullets: [
      "Studied core computer science and IT concepts, specializing in database systems, computer networking, and system design.",
      "Developed the eMediCard Capstone web and mobile health card management system using React Native, Expo, and Convex NoSQL.",
      "Implemented mobile QR code scanning verification and shared TypeScript schema validations across backend functions."
    ],
    highlight: false,
    icon: GraduationCap,
    accentColor: "text-sky",
    borderColor: "hover:border-sky/30",
    innerGlowColor: "group-hover:bg-sky/10",
    glowColor: "bg-sky/5",
    dotColor: "before:bg-sky",
    tag: "Academic Program",
    techTags: ["Database Management", "Systems Analysis", "Network Design", "Software Engineering", "OOP"]
  },
  {
    id: "mcdonalds",
    type: "experience",
    role: "Crew Trainer",
    company: "McDonald's Philippines",
    date: "Sep 2022 — Apr 2026",
    bullets: [
      "Trained and mentored new crew members on kitchen stations, operational procedures, and safety guidelines.",
      "Performed standard kitchen production and service crew duties while executing crew trainer responsibilities.",
      "Demonstrated strong time management by balancing shifts and training duties alongside full-time college studies."
    ],
    highlight: false,
    icon: McDonaldsIcon,
    accentColor: "text-amber",
    borderColor: "hover:border-amber/30",
    innerGlowColor: "group-hover:bg-amber/10",
    glowColor: "bg-amber/5",
    dotColor: "before:bg-amber",
    tag: "Operational Focus",
    techTags: ["Operations", "Time Management", "Teamwork"]
  },
  {
    id: "shs",
    type: "education",
    role: "TVL-ICT (CSS) NCII",
    company: "Cabantian National High School",
    date: "2020 — 2022",
    bullets: [
      "Completed the Technical-Vocational-Livelihood (TVL) Track, specializing in the Information and Communications Technology (ICT) Strand.",
      "Studied networking principles and completed hands-on training in computer systems installation, diagnostics, and network configuration.",
      "Completed specialized Computer Systems Servicing (CSS) NC II server installation training at Interface Computer College through the Joint Delivery Voucher Program (JDVP), successfully earning the TESDA NC II credential."
    ],
    highlight: false,
    icon: Code,
    accentColor: "text-[#a78bfa]",
    borderColor: "hover:border-[#a78bfa]/30",
    innerGlowColor: "group-hover:bg-[#a78bfa]/10",
    glowColor: "bg-[#a78bfa]/5",
    dotColor: "before:bg-[#a78bfa]",
    tag: "Secondary Education",
    techTags: ["Computer Systems Servicing", "Computer Networking", "Server Configuration"]
  },
];
---

<MainLayout
  title="Sean Paul Lapasanda | Full Stack Software Engineer"
  description="Portfolio of Sean Paul S. Lapasanda, a full-stack engineer building Laravel, React, React Native, and TypeScript systems."
>
  <header class="page-shell sticky top-0 z-50 py-3 sm:py-4">
    <nav
      class="glass grid min-w-0 grid-cols-[minmax(0,1fr)_auto] items-center gap-3 rounded-2xl px-3 py-3 text-sm text-ink sm:rounded-full sm:px-5 sm:py-3.5 md:flex md:flex-wrap md:justify-between"
      aria-label="Primary navigation"
    >
      <a href="#top" id="logo" class="flex min-w-0 items-center gap-2 font-semibold group">
        <Logo class="size-9 shrink-0 sm:size-8" />
        <span class="font-display text-sm font-bold tracking-tight text-ink transition-colors group-hover:text-mint sm:text-base">Ichihara</span>
      </a>
      <div class="hidden items-center gap-1.5 md:flex">
        {
          navItems.map((item) => (
            <a
              id={`nav-link-${item.toLowerCase()}`}
              href={`#${item.toLowerCase()}`}
              class="rounded-full px-4 py-2 text-muted font-medium transition hover:bg-white/8 hover:text-ink"
            >
              {item}
            </a>
          ))
        }
      </div>
      <div class="flex min-w-0 items-center justify-end gap-2">
        <a
          id="nav-contact-button"
          href="#contact"
          aria-label="Contact Ichihara"
          class="hidden min-h-10 shrink-0 items-center justify-center gap-2 rounded-full bg-ink px-4 py-2 text-xs font-bold text-bg-deep transition hover:-translate-y-0.5 hover:bg-mint hover:shadow-lg hover:shadow-mint/20 md:inline-flex"
        >
          <Mail size={14} />
          <span>Let's Talk</span>
        </a>
        <button
          id="theme-toggle"
          type="button"
          class="theme-toggle grid size-11 shrink-0 place-items-center rounded-full text-ink transition hover:-translate-y-0.5 hover:border-mint hover:text-mint sm:size-10"
          aria-label="Switch color theme"
          aria-pressed="false"
        >
          <Sun size={17} class="theme-icon-sun" />
          <Moon size={17} class="theme-icon-moon" />
          <span class="sr-only">Toggle color theme</span>
        </button>
        <button
          id="mobile-menu-toggle"
          type="button"
          class="inline-flex min-h-11 max-w-full shrink-0 items-center justify-center gap-2 rounded-full border border-white/8 bg-white/[0.03] px-3.5 text-xs font-bold text-ink transition hover:border-mint hover:text-mint md:hidden"
          aria-controls="mobile-nav-panel"
          aria-expanded="false"
        >
          <Menu size={16} />
          <span id="mobile-menu-label">Menu</span>
        </button>
      </div>
      <div
        id="mobile-nav-panel"
        class="col-span-2 hidden w-full border-t border-white/8 pt-3 md:hidden"
      >
        <div class="grid gap-2">
        {
          navItems.map((item) => (
            <a
              id={`mobile-nav-link-${item.toLowerCase()}`}
              href={`#${item.toLowerCase()}`}
              class="mobile-nav-link inline-flex min-h-11 items-center justify-between rounded-xl border border-white/6 bg-white/[0.02] px-4 text-sm font-semibold text-muted transition hover:border-mint hover:text-mint"
            >
              <span>{item}</span>
              <ArrowUpRight size={15} />
            </a>
          ))
        }
        </div>
      </div>
    </nav>
  </header>

  <main id="top">
    <!-- Hero Section -->
    <section class="page-shell grid items-center gap-8 py-6 sm:gap-10 sm:py-8 lg:min-h-[calc(100svh-5.5rem)] lg:grid-cols-[1.1fr_0.9fr] lg:py-6">
      <div class="scroll-reveal">
        <div class="mb-4 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-xs text-ink/90">
          <Globe size={14} class="text-mint" />
          <span>Available for remote & hybrid opportunities</span>
        </div>
        <h1 class="fluid-heading max-w-4xl font-display font-extrabold leading-[1.05] tracking-tight text-ink sm:text-balance">
          <span class="block sm:inline">Full-stack developer</span>
          <span class="block sm:inline"> building <span class="text-mint">web</span> and</span>
          <span class="block sm:inline"> <span class="text-sky">mobile</span> applications.</span>
        </h1>
        <p class="mt-4 max-w-2xl text-base leading-relaxed text-muted">
          I build web systems and mobile apps that digitize real operations — backed by structured databases, secure APIs, and clean user interfaces.
        </p>
        <div class="mt-6 flex flex-col gap-3 sm:flex-row sm:flex-wrap sm:gap-4">
          <a
            id="hero-cta-projects"
            href="#projects"
            class="inline-flex min-h-11 items-center justify-center gap-2 rounded-full bg-mint px-5.5 py-3 text-sm font-extrabold text-[#07100d] transition hover:-translate-y-0.5 hover:bg-[#7af0c8] hover:shadow-lg hover:shadow-mint/20 sm:justify-start"
          >
            <Briefcase size={18} />
            View Work
          </a>
          <a
            id="hero-cta-cv"
            href={cvHref}
            target="_blank"
            rel="noreferrer"
            class="inline-flex min-h-11 items-center justify-center gap-2 rounded-full bg-mint px-5.5 py-3 text-sm font-extrabold text-[#07100d] transition hover:-translate-y-0.5 hover:bg-[#7af0c8] hover:shadow-lg hover:shadow-mint/20 sm:justify-start"
          >
            <FileText size={18} />
            View CV
          </a>
        </div>
        <div class="mt-8 grid grid-cols-2 items-center gap-3 text-xs text-muted sm:flex sm:flex-wrap sm:gap-4">
          <span class="inline-flex items-center gap-2 rounded-full border border-white/6 bg-white/[0.02] px-3.5 py-2">
            <MapPin size={14} class="text-sky" />
            Philippines
          </span>
          {
            profileLinks.map(({ id, label, href, icon: Icon }) => (
              <a
                id={id}
                href={href}
                target={href.startsWith("http") ? "_blank" : undefined}
                rel={href.startsWith("http") ? "noreferrer" : undefined}
                class="inline-flex min-h-10 items-center gap-2 rounded-full border border-white/6 bg-white/[0.01] px-3.5 py-2 hover:border-mint hover:text-mint"
              >
                <Icon size={14} />
                {label}
              </a>
            ))
          }
          
          <!-- Interactive CLI Toggle Button (UX Pattern C) -->
          <button
            id="hero-cli-toggle"
            class="col-span-2 inline-flex min-h-10 items-center justify-center gap-2 rounded-full border border-amber/30 bg-white/[0.02] px-3.5 py-2 font-mono text-amber transition-all duration-300 hover:scale-102 hover:border-mint hover:text-mint cursor-pointer select-none sm:col-span-1 sm:justify-start"
            aria-label="Toggle CLI Terminal"
          >
            <span id="hero-cli-icon-terminal" class="hidden items-center">
              <Terminal size={14} class="text-mint shrink-0" />
            </span>
            <span id="hero-cli-icon-profile" class="flex items-center">
              <User size={14} class="text-sky shrink-0" />
            </span>
            <span id="hero-cli-text" class="text-ink">Close CLI</span>
          </button>
        </div>
      </div>

      <!-- Right Column: Interactive CLI & profile -->
      <div class="scroll-reveal" style="transition-delay: 150ms;">
        <DeveloperDeck />
      </div>
    </section>

    <!-- GitHub Activity Section (Directly Below Landing/Hero) -->
    <section id="activity" class="page-shell pt-8 pb-14 sm:pt-10 sm:pb-16 lg:pt-12 lg:pb-20">
      <div class="mb-8 max-w-3xl scroll-reveal">
        <h2 class="font-display text-3xl font-extrabold leading-tight text-ink sm:text-4xl">
          GitHub Activity
        </h2>
        <p class="mt-2.5 max-w-2xl text-sm leading-relaxed text-muted">
          Public commits, repository contributions, and daily development cadence.
        </p>
      </div>
      <GithubContributions />
    </section>

    <!-- Projects Section -->
    <section id="projects" class="relative border-y border-white/10 bg-bg-card pt-8 pb-14 sm:pt-10 sm:pb-16 lg:pt-12 lg:pb-20">
      <div class="absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-mint/30 to-transparent"></div>
      <div class="absolute left-1/2 top-24 h-72 w-3/4 -translate-x-1/2 rounded-full bg-mint/5 blur-3xl pointer-events-none"></div>

      <div class="page-shell relative z-10">
        <div class="mb-8 max-w-3xl scroll-reveal">
          <h2 class="font-display text-3xl font-extrabold leading-tight text-ink sm:text-4xl">
            Projects
          </h2>
          <p class="mt-2.5 max-w-2xl text-sm leading-relaxed text-muted">
            Systems built across internship, capstone, and personal development in web and mobile.
          </p>
        </div>

        <!-- Project Cards Grid (3 Columns) -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5 sm:gap-6">
          
          <!-- Card 1: F.M. Morata Customs Brokerage & Law Firm (Operations System) -->
          <article class="group scroll-reveal project-card relative flex flex-col justify-between overflow-hidden rounded-2xl bg-bg-deep/55 shadow-xl transition-all duration-300 hover:-translate-y-1.5 hover:shadow-2xl">
            <!-- Dedicated border overlay that always paints on top of scaling images -->
            <div class="pointer-events-none absolute inset-0 z-20 rounded-2xl border border-white/10 transition-colors duration-300 group-hover:border-mint/30"></div>
            <div>
              <!-- Visual Screenshot Preview Frame -->
              <div class="aspect-[2.11/1] w-full overflow-hidden border-b border-white/8 bg-[#070b0e] relative">
                <Image
                  src={morataLandingImage}
                  alt="F.M. Morata Customs Brokerage & Law Firm screenshot"
                  class="size-full object-cover object-top transition-transform duration-500 group-hover:scale-105"
                  loading="lazy"
                />
              </div>

              <!-- Card Body Content -->
              <div class="p-5 sm:p-6">
                <!-- Category Pills -->
                <div class="flex flex-wrap items-center gap-2">
                  <span class="rounded-full border border-mint/30 bg-mint/5 px-2.5 py-0.5 font-mono text-[10px] font-bold uppercase tracking-wider text-mint">
                    Web Application
                  </span>
                  <span class="rounded-full border border-mint/30 bg-mint/5 px-2.5 py-0.5 font-mono text-[10px] font-bold uppercase tracking-wider text-mint">
                    Logistics
                  </span>
                  <span class="rounded-full border border-mint/30 bg-mint/5 px-2.5 py-0.5 font-mono text-[10px] font-bold uppercase tracking-wider text-mint">
                    Legal
                  </span>
                </div>

                <!-- Title & Description -->
                <h3 class="mt-3.5 font-display text-xl font-bold text-ink">
                  F.M. Morata Customs Brokerage &amp; Law Firm
                </h3>
                <p class="mt-2 text-xs sm:text-sm leading-relaxed text-muted">
                  Operations system for brokerage and law firm workflows — import/export transaction tracking, legal document records, and archiving.
                </p>
              </div>
            </div>

            <!-- CTA Badge & Tech Stack -->
            <div class="p-5 pt-0 sm:p-6 sm:pt-0 space-y-3">
              <p class="font-mono text-[10.5px] text-muted/80 truncate">
                React • Tailwind CSS • Laravel • MySQL
              </p>
              <div
                class="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-white/[0.04] border border-white/10 py-2.5 text-xs font-bold text-muted cursor-default select-none shadow-xs"
              >
                <Lock size={13} class="opacity-75 text-mint" />
                <span class="text-ink/90">Internship Work</span>
              </div>
            </div>
          </article>

          <!-- Card 2: F.M. Morata Customs Brokerage & Law Firm (Static Website) -->
          <article class="group scroll-reveal project-card relative flex flex-col justify-between overflow-hidden rounded-2xl bg-bg-deep/55 shadow-xl transition-all duration-300 hover:-translate-y-1.5 hover:shadow-2xl" style="transition-delay: 80ms;">
            <!-- Dedicated border overlay that always paints on top of scaling images -->
            <div class="pointer-events-none absolute inset-0 z-20 rounded-2xl border border-white/10 transition-colors duration-300 group-hover:border-mint/30"></div>
            <div>
              <!-- Visual Screenshot Preview Frame -->
              <div class="aspect-[2.11/1] w-full overflow-hidden border-b border-white/8 bg-[#070b0e] relative">
                <Image
                  src={fmmCbsLandingImage}
                  alt="F.M. Morata Customs Brokerage & Law Firm landing screen"
                  class="size-full object-cover object-top transition-transform duration-500 group-hover:scale-105"
                  loading="lazy"
                />
              </div>

              <!-- Card Body Content -->
              <div class="p-5 sm:p-6">
                <!-- Category Pills -->
                <div class="flex flex-wrap items-center gap-2">
                  <span class="rounded-full border border-mint/30 bg-mint/5 px-2.5 py-0.5 font-mono text-[10px] font-bold uppercase tracking-wider text-mint">
                    Static Website
                  </span>
                </div>

                <!-- Title & Description -->
                <h3 class="mt-3.5 font-display text-xl font-bold text-ink">
                  F.M. Morata Customs Brokerage &amp; Law Firm
                </h3>
                <p class="mt-2 text-xs sm:text-sm leading-relaxed text-muted">
                  Corporate website for a customs brokerage firm — showcasing services, destinations, and inquiries.
                </p>
              </div>
            </div>

            <!-- CTA Link & Tech Stack -->
            <div class="p-5 pt-0 sm:p-6 sm:pt-0 space-y-3">
              <p class="font-mono text-[10.5px] text-muted/80 truncate">
                React • Tailwind CSS • Resend
              </p>
              <a
                href="https://blog.fmmcbs.com"
                target="_blank"
                rel="noreferrer"
                class="project-card-btn inline-flex w-full items-center justify-center gap-2 rounded-xl bg-white/5 border border-white/10 py-2.5 text-xs font-bold text-ink transition hover:bg-mint hover:text-[#07100d] hover:border-mint shadow-sm"
              >
                <span>Visit</span>
                <ArrowUpRight size={14} />
              </a>
            </div>
          </article>

          <!-- Card 3: eMediCard -->
          <article class="group scroll-reveal project-card relative flex flex-col justify-between overflow-hidden rounded-2xl bg-bg-deep/55 shadow-xl transition-all duration-300 hover:-translate-y-1.5 hover:shadow-2xl" style="transition-delay: 150ms;">
            <!-- Dedicated border overlay that always paints on top of scaling images -->
            <div class="pointer-events-none absolute inset-0 z-20 rounded-2xl border border-white/10 transition-colors duration-300 group-hover:border-mint/30"></div>
            <div>
              <!-- Visual Screenshot Preview Frame -->
              <div class="aspect-[2.11/1] w-full overflow-hidden border-b border-white/8 bg-[#070b0e] relative">
                <Image
                  src={emedicardLandingImage}
                  alt="eMediCard digital platform landing page"
                  class="size-full object-cover object-top transition-transform duration-500 group-hover:scale-105"
                  loading="lazy"
                />
              </div>

              <!-- Card Body Content -->
              <div class="p-5 sm:p-6">
                <!-- Category Pills -->
                <div class="flex flex-wrap items-center gap-2">
                  <span class="rounded-full border border-mint/30 bg-mint/5 px-2.5 py-0.5 font-mono text-[10px] font-bold uppercase tracking-wider text-mint">
                    Web Application
                  </span>
                  <span class="rounded-full border border-mint/30 bg-mint/5 px-2.5 py-0.5 font-mono text-[10px] font-bold uppercase tracking-wider text-mint">
                    Healthcare
                  </span>
                  <span class="rounded-full border border-mint/30 bg-mint/5 px-2.5 py-0.5 font-mono text-[10px] font-bold uppercase tracking-wider text-mint">
                    City System
                  </span>
                </div>

                <!-- Title & Description -->
                <h3 class="mt-3.5 font-display text-xl font-bold text-ink">
                  eMediCard
                </h3>
                <p class="mt-2 text-xs sm:text-sm leading-relaxed text-muted">
                  Digital health card platform for Davao City — applicant submissions, document verification, and QR cards.
                </p>
              </div>
            </div>

            <!-- CTA Trigger & Tech Stack -->
            <div class="p-5 pt-0 sm:p-6 sm:pt-0 space-y-3">
              <p class="font-mono text-[10.5px] text-muted/80 truncate">
                React Native • Expo • Next.js • Convex • Clerk
              </p>
              <button
                type="button"
                class="project-card-btn project-modal-trigger inline-flex w-full items-center justify-center gap-2 rounded-xl bg-white/5 border border-white/10 py-2.5 text-xs font-bold text-ink transition hover:bg-mint hover:text-[#07100d] hover:border-mint cursor-pointer shadow-sm"
                data-project-modal-target="emedicard-modal"
                aria-haspopup="dialog"
              >
                <span>Details</span>
                <ArrowUpRight size={14} />
              </button>
            </div>
          </article>

          <!-- Card 4: Readorama -->
          <article class="group scroll-reveal project-card relative flex flex-col justify-between overflow-hidden rounded-2xl bg-bg-deep/55 shadow-xl transition-all duration-300 hover:-translate-y-1.5 hover:shadow-2xl" style="transition-delay: 220ms;">
            <!-- Dedicated border overlay that always paints on top of scaling images -->
            <div class="pointer-events-none absolute inset-0 z-20 rounded-2xl border border-white/10 transition-colors duration-300 group-hover:border-mint/30"></div>
            <div>
              <!-- Visual Screenshot Preview Frame -->
              <div class="aspect-[2.11/1] w-full overflow-hidden border-b border-white/8 bg-[#070b0e] relative">
                <Image
                  src={readoramaPreviewImage}
                  alt="Readorama mobile marketplace UI/UX design presentation"
                  class="size-full object-cover object-center transition-transform duration-500 group-hover:scale-105"
                  loading="lazy"
                />
              </div>

              <!-- Card Body Content -->
              <div class="p-5 sm:p-6">
                <!-- Category Pills -->
                <div class="flex flex-wrap items-center gap-2">
                  <span class="rounded-full border border-mint/30 bg-mint/5 px-2.5 py-0.5 font-mono text-[10px] font-bold uppercase tracking-wider text-mint">
                    UI/UX Design
                  </span>
                  <span class="rounded-full border border-mint/30 bg-mint/5 px-2.5 py-0.5 font-mono text-[10px] font-bold uppercase tracking-wider text-mint">
                    Figma
                  </span>
                </div>

                <!-- Title & Description -->
                <h3 class="mt-3.5 font-display text-xl font-bold text-ink">
                  Readorama
                </h3>
                <p class="mt-2 text-xs sm:text-sm leading-relaxed text-muted">
                  Mobile marketplace UI/UX design for book exchange and acquisition — community trade flows, categorized discovery, and checkout prototypes in Figma.
                </p>
              </div>
            </div>

            <!-- CTA Trigger & Tech Stack -->
            <div class="p-5 pt-0 sm:p-6 sm:pt-0 space-y-3">
              <p class="font-mono text-[10.5px] text-muted/80 truncate">
                Figma • UI/UX Design • Wireframing • Prototyping
              </p>
              <button
                type="button"
                class="project-card-btn project-modal-trigger inline-flex w-full items-center justify-center gap-2 rounded-xl bg-white/5 border border-white/10 py-2.5 text-xs font-bold text-ink transition hover:bg-mint hover:text-[#07100d] hover:border-mint cursor-pointer shadow-sm"
                data-project-modal-target="readorama-modal"
                aria-haspopup="dialog"
              >
                <span>Details</span>
                <ArrowUpRight size={14} />
              </button>
            </div>
          </article>

        </div>
      </div>
    </section>

    <!-- Technical Stack Section -->
    <section id="skills" class="page-shell pt-8 pb-14 sm:pt-10 sm:pb-16 lg:pt-12 lg:pb-20">
      <div class="mb-8 max-w-3xl scroll-reveal">
        <h2 class="font-display text-3xl font-extrabold leading-tight text-ink sm:text-4xl">
          Tech Stack
        </h2>
        <p class="mt-2.5 max-w-2xl text-sm leading-relaxed text-muted">
          Languages, frameworks, and tools I use to build across frontend, backend, and mobile.
        </p>
      </div>
      <div class="grid grid-cols-[repeat(auto-fit,minmax(min(15rem,100%),1fr))] gap-5">
        {
          skills.map(({ title, icon: Icon, items }) => (
            <article class="scroll-reveal glass rounded-2xl p-5 hover:-translate-y-1 hover:border-mint/20 transition-all duration-300">
              <div class="mb-6 flex items-center justify-between">
                <h3 class="text-sm font-bold text-ink font-display uppercase tracking-wide">{title}</h3>
                <Icon size={20} class="text-mint" />
              </div>
              <div class="flex flex-wrap gap-1.5">
                {
                  items.map((item) => (
                    <span class="inline-flex items-center gap-1.5 rounded border border-white/5 bg-white/4 px-2 py-1 text-[11px] font-medium text-muted">
                      {techIcons[item] ? (
                        <img
                          src={techIcons[item]}
                          alt=""
                          aria-hidden="true"
                          data-tech={item}
                          class="tech-icon size-3.5 shrink-0"
                          loading="lazy"
                        />
                      ) : (
                        <span class="grid size-3.5 shrink-0 place-items-center rounded-[4px] border border-white/10 bg-white/5 font-mono text-[8px] font-black text-mint/80">
                          {item.slice(0, 1)}
                        </span>
                      )}
                      {item}
                    </span>
                  ))
                }
              </div>
            </article>
          ))
        }
      </div>
    </section>

    <!-- Experience Section -->
    <section id="experience" class="fluid-section relative border-y border-white/10 bg-bg-card">
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_bottom,rgba(102,224,181,0.04),transparent_50%)] pointer-events-none"></div>

      <div class="page-shell relative z-10">
        <div class="mb-12 scroll-reveal text-center">
          <p class="section-kicker">Journey</p>
          <h2 class="mt-3 font-display text-3xl font-extrabold text-ink sm:text-4xl">
            Work &amp; Education
          </h2>
        </div>

        <!-- Alternating Split Timeline Container -->
        <div class="relative space-y-12 lg:space-y-6 mt-10">
          <!-- Vertical Center Timeline Line -->
          <div class="timeline-rail absolute left-3.5 lg:left-1/2 lg:-translate-x-1/2 top-2 bottom-2"></div>

          {
            timelineItems.map((item) => (
              <div class="relative flex flex-col lg:flex-row items-center justify-between w-full group/timeline">
                <!-- Center Dot Node -->
                <div class="absolute left-3.5 lg:left-1/2 lg:-translate-x-1/2 top-[38px] lg:top-1/2 lg:-translate-y-1/2 z-10 flex flex-col items-center">
                  <div class={`w-3.5 h-3.5 rounded-full border-2 transition-transform duration-300 group-hover/timeline:scale-125 bg-bg-deep ${
                    item.highlight 
                      ? `${item.accentColor} border-current shadow-[0_0_8px_rgba(255,255,255,0.15)]`
                      : `border-white/20 group-hover/timeline:border-current ${item.accentColor}`
                  }`}></div>
                </div>

                <!-- Left Column: Education details on left, hidden spacer for experience -->
                <div class={`w-full lg:w-[calc(50%-2.5rem)] order-1 ${item.type === 'education' ? 'block pl-10 lg:pl-0' : 'hidden lg:block lg:opacity-0 lg:pointer-events-none'}`}>
                  {item.type === 'education' && (
                    <article class={`scroll-reveal glass rounded-2xl border border-white/8 p-4 sm:p-5 ${item.borderColor} hover:-translate-y-0.5 transition-all duration-300 relative overflow-hidden group/card flex flex-col justify-between`}>
                      <span class={`absolute -right-8 -top-8 size-20 ${item.glowColor} rounded-full blur-xl ${item.innerGlowColor} transition-all duration-300 pointer-events-none`}></span>
                      
                      <div>
                        <!-- Header -->
                        <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
                          <div class="flex min-w-0 items-center gap-3">
                            <span class={`grid size-9 place-items-center rounded-xl bg-white/[0.03] border border-white/8 ${item.accentColor}`}>
                              <item.icon size={18} />
                            </span>
                            <span class="text-[9px] font-mono font-bold tracking-widest text-muted uppercase">
                              {item.tag}
                            </span>
                          </div>
                          <span class="w-fit rounded-full border border-white/5 bg-white/5 px-2 py-0.5 font-mono text-[9px] text-muted">
                            {item.date}
                          </span>
                        </div>

                        <!-- Title & Company -->
                        <div class="mt-3.5 text-left">
                          <h3 class={`text-base font-bold font-display transition-colors group-hover/card:${item.accentColor} ${item.highlight ? item.accentColor : "text-ink"}`}>
                            {item.role}
                          </h3>
                          <p class="text-xs text-muted font-medium mt-1">{item.company}</p>
                        </div>

                        <!-- Bullets -->
                        <ul class="mt-4 space-y-2.5 text-xs text-muted list-none leading-relaxed text-left">
                          {item.bullets.map((bullet) => (
                            <li class={`relative pl-4 before:absolute before:left-0 before:top-2 before:size-1.5 before:rounded-full ${item.dotColor}`}>
                              {bullet}
                            </li>
                          ))}
                        </ul>
                      </div>

                      <!-- Tech Tags -->
                      {item.techTags && item.techTags.length > 0 && (
                        <div class="mt-5 flex flex-wrap gap-1 justify-start">
                          {item.techTags.map((tech) => (
                            <span class="rounded bg-white/4 border border-white/5 px-2 py-0.5 text-[10px] font-medium text-muted transition-colors group-hover/card:border-current group-hover/card:text-ink">
                              {tech}
                            </span>
                          ))}
                        </div>
                      )}
                    </article>
                  )}
                </div>

                <!-- Right Column: Experience details on right, hidden spacer for education -->
                <div class={`w-full lg:w-[calc(50%-2.5rem)] order-2 ${item.type === 'experience' ? 'block pl-10 lg:pl-0' : 'hidden lg:block lg:opacity-0 lg:pointer-events-none'}`}>
                  {item.type === 'experience' && (
                    <article class={`scroll-reveal glass rounded-2xl border border-white/8 p-4 sm:p-5 ${item.borderColor} hover:-translate-y-0.5 transition-all duration-300 relative overflow-hidden group/card flex flex-col justify-between`}>
                      <span class={`absolute -right-8 -top-8 size-20 ${item.glowColor} rounded-full blur-xl ${item.innerGlowColor} transition-all duration-300 pointer-events-none`}></span>
                      
                      <div>
                        <!-- Header -->
                        <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
                          <div class="flex min-w-0 items-center gap-3">
                            <span class={`grid size-9 place-items-center rounded-xl bg-white/[0.03] border border-white/8 ${item.accentColor}`}>
                              <item.icon size={18} />
                            </span>
                            <span class="text-[9px] font-mono font-bold tracking-widest text-muted uppercase">
                              {item.tag}
                            </span>
                          </div>
                          <span class="w-fit rounded-full border border-white/5 bg-white/5 px-2 py-0.5 font-mono text-[9px] text-muted">
                            {item.date}
                          </span>
                        </div>

                        <!-- Title & Company -->
                        <div class="mt-3.5 text-left">
                          <h3 class={`text-base font-bold font-display transition-colors group-hover/card:${item.accentColor} ${item.highlight ? item.accentColor : "text-ink"}`}>
                            {item.role}
                          </h3>
                          <p class="text-xs text-muted font-medium mt-1">{item.company}</p>
                        </div>

                        <!-- Bullets -->
                        <ul class="mt-4 space-y-2.5 text-xs text-muted list-none leading-relaxed text-left">
                          {item.bullets.map((bullet) => (
                            <li class={`relative pl-4 before:absolute before:left-0 before:top-2 before:size-1.5 before:rounded-full ${item.dotColor}`}>
                              {bullet}
                            </li>
                          ))}
                        </ul>
                      </div>

                      <!-- Tech Tags -->
                      {item.techTags && item.techTags.length > 0 && (
                        <div class="mt-5 flex flex-wrap gap-1 justify-start">
                          {item.techTags.map((tech) => (
                            <span class="rounded bg-white/4 border border-white/5 px-2 py-0.5 text-[10px] font-medium text-muted transition-colors group-hover/card:border-current group-hover/card:text-ink">
                              {tech}
                            </span>
                          ))}
                        </div>
                      )}
                    </article>
                  )}
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="page-shell fluid-section">
      <div class="glass relative grid gap-10 overflow-hidden rounded-2xl border border-white/8 p-5 transition-all hover:border-white/12 sm:p-7 md:grid-cols-[1.1fr_0.9fr] md:p-10">
        <!-- Glow background -->
        <span class="absolute -left-16 -top-16 size-48 bg-amber/5 rounded-full blur-3xl pointer-events-none"></span>

        <div class="scroll-reveal">
          <p class="section-kicker">Get In Touch</p>
          <h2 class="mt-3 font-display text-3xl font-extrabold leading-tight text-ink sm:text-4xl">
            Let's build something practical together.
          </h2>
          <p class="mt-5 text-sm leading-relaxed text-muted">
            I am open to junior full-stack, frontend, backend, or mobile developer roles, with availability for remote or hybrid positions.
          </p>

          <div class="mt-8 max-w-lg border-t border-white/8 pt-5">
            <p class="text-[10px] font-extrabold uppercase tracking-[0.16em] text-muted">Current Focus</p>
            <p class="mt-2 text-sm leading-relaxed text-muted">
              Building practical web dashboards, backend APIs, and mobile workflows with Laravel, React, and React Native.
            </p>
          </div>
        </div>

        <!-- Contact Options -->
        <div class="scroll-reveal flex flex-col justify-between rounded-2xl border border-white/6 bg-white/[0.02] p-5 md:p-6" style="transition-delay: 150ms;">
          <div>
            <p class="text-[10px] font-extrabold uppercase tracking-[0.16em] text-mint">Preferred Contact</p>
            <h3 class="mt-3 font-display text-2xl font-extrabold leading-tight text-ink">
              Reach out directly.
            </h3>
            <p class="mt-4 text-sm leading-relaxed text-muted">
              For recruiter messages, collaboration, or project questions, send a direct email and I will reply when available.
            </p>
          </div>

          <div class="mt-8 space-y-3">
            <a
              href="mailto:seanpaullapasanda@gmail.com?subject=Portfolio%20Opportunity"
              class="inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-full bg-mint px-5 py-3 text-sm font-extrabold text-[#07100d] transition hover:-translate-y-0.5 hover:bg-[#7af0c8] hover:shadow-lg hover:shadow-mint/20"
            >
              <Mail size={18} />
              Send Email
            </a>

            <div class="grid gap-3 sm:grid-cols-2">
              <a
                href="https://www.linkedin.com/in/sean-paul-lapasanda-219117241/"
                target="_blank"
                rel="noreferrer"
                class="inline-flex min-h-11 items-center justify-center gap-2 rounded-full border border-white/8 bg-[#0b0f14]/40 px-4 py-2.5 text-xs font-bold text-ink transition hover:border-sky hover:text-sky"
              >
                <ExternalLink size={15} />
                LinkedIn
              </a>
              <a
                href={cvHref}
                target="_blank"
                rel="noreferrer"
                class="inline-flex min-h-11 items-center justify-center gap-2 rounded-full border border-white/8 bg-[#0b0f14]/40 px-4 py-2.5 text-xs font-bold text-ink transition hover:border-amber hover:text-amber"
              >
                <FileText size={15} />
                View CV
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- eMediCard Case Study Modal (Unified Mint Theme) -->
  <div
    id="emedicard-modal"
    class="project-modal fixed inset-0 z-[100] hidden items-center justify-center bg-black/80 p-4 backdrop-blur-md sm:p-6"
    role="dialog"
    aria-modal="true"
    aria-labelledby="emedicard-modal-title"
  >
    <div class="project-modal-panel max-h-[90vh] w-full max-w-5xl overflow-y-auto overflow-x-hidden rounded-2xl border border-slate-200/90 bg-white text-slate-950 shadow-2xl transition-all dark:border-white/10 dark:bg-[#0c1015] dark:text-ink">
      <!-- Modal Header -->
      <div class="project-modal-header sticky top-0 z-20 flex flex-col gap-3 border-b border-slate-200/80 bg-white/95 px-6 py-4.5 backdrop-blur-md dark:border-white/8 dark:bg-[#0c1015]/95 sm:flex-row sm:items-center sm:justify-between">
        <div class="space-y-1">
          <div class="flex items-center gap-2">
            <h2 id="emedicard-modal-title" class="font-display text-2xl font-bold tracking-tight text-ink">
              eMediCard
            </h2>
            <span class="rounded-md border border-mint/30 bg-mint/10 px-2 py-0.5 font-mono text-[10px] font-semibold text-mint">
              Capstone Project
            </span>
          </div>
          <p class="text-xs text-muted">
            Academic Capstone Project • Digital Health Certificate &amp; Sanitary Permit Application Platform
          </p>
        </div>
        <div class="flex items-center gap-2.5">
          <button
            type="button"
            class="project-modal-close inline-flex size-8 items-center justify-center rounded-lg border border-transparent text-muted transition-colors hover:border-slate-200 hover:bg-slate-100 hover:text-ink dark:hover:border-white/10 dark:hover:bg-white/10 dark:hover:text-ink cursor-pointer"
            aria-label="Close eMediCard details"
          >
            <X size={16} />
          </button>
        </div>
      </div>

      <!-- Modal Body Grid (strictly bounded) -->
      <div class="grid min-w-0 gap-6 p-6 lg:grid-cols-[minmax(0,0.85fr)_minmax(0,1.15fr)] lg:gap-8">
        <!-- Left Column: Scope & Overview (Typography-First) -->
        <div class="min-w-0 flex flex-col justify-between space-y-6">
          <div class="space-y-6">
            <!-- Key Engineering Highlights -->
            <div class="space-y-3.5">
              <div class="flex items-center justify-between border-b border-slate-200/60 pb-2 dark:border-white/8 min-h-[29px]">
                <span class="font-mono text-[10px] font-bold uppercase tracking-widest text-mint">
                  Key Engineering Highlights
                </span>
              </div>
              <ul class="space-y-4">
                <li class="space-y-1">
                  <div class="flex items-center gap-2">
                    <span class="size-1.5 shrink-0 rounded-full bg-mint"></span>
                    <h3 class="font-sans text-sm font-semibold text-ink">Mobile Application &amp; Multi-Step Submissions</h3>
                  </div>
                  <p class="pl-3.5 text-xs sm:text-sm text-muted leading-relaxed">
                    Engineered cross-platform mobile submission workflows with category selection, automated document checklists, and secure multi-file upload stages in React Native (Expo).
                  </p>
                </li>

                <li class="space-y-1">
                  <div class="flex items-center gap-2">
                    <span class="size-1.5 shrink-0 rounded-full bg-mint"></span>
                    <h3 class="font-sans text-sm font-semibold text-ink">Web Admin Document Verification Portal</h3>
                  </div>
                  <p class="pl-3.5 text-xs sm:text-sm text-muted leading-relaxed">
                    Collaborated with a teammate to build a dedicated administrative review dashboard using Next.js, enabling city health department staff to review citizen requirements, verify laboratory results, and manage application approvals.
                  </p>
                </li>

                <li class="space-y-1">
                  <div class="flex items-center gap-2">
                    <span class="size-1.5 shrink-0 rounded-full bg-mint"></span>
                    <h3 class="font-sans text-sm font-semibold text-ink">Digital Certificate &amp; Instant QR Verification</h3>
                  </div>
                  <p class="pl-3.5 text-xs sm:text-sm text-muted leading-relaxed">
                    Implemented official digital health card and certificate generation featuring tamper-resistant QR codes for real-time field validation by city inspectors.
                  </p>
                </li>
              </ul>
            </div>

            <!-- Subtle Divider -->
            <div class="border-t border-slate-200/70 dark:border-white/8"></div>

            <!-- Project Concept -->
            <div class="space-y-2">
              <span class="font-mono text-[10px] font-bold uppercase tracking-widest text-muted">
                About the System
              </span>
              <p class="text-xs sm:text-sm text-muted leading-relaxed">
                eMediCard modernizes local government health services by replacing slow, paper-based health certificate queues with a unified citizen mobile app and back-office verification portal.
              </p>
            </div>
          </div>

          <!-- Tech Stack Footer -->
          <div class="flex flex-wrap items-center justify-between gap-2 border-t border-slate-200/70 pt-4 dark:border-white/8 text-xs text-muted">
            <div class="flex items-center gap-2">
              <span class="font-mono text-[10px] font-bold uppercase tracking-wider text-muted">Stack:</span>
              <span>React Native • Expo • Next.js • TypeScript • Convex • Clerk</span>
            </div>
          </div>
        </div>

        <!-- Right Column: System Interfaces Showcase (Strictly Contained) -->
        <div class="min-w-0 space-y-5">
          <!-- Top Unified Header Bar (Selected Pages & Screens Notice) -->
          <div class="flex items-center justify-between border-b border-slate-200/60 pb-2 dark:border-white/8 min-h-[29px]">
            <span class="font-mono text-[10px] font-bold uppercase tracking-widest text-ink">
              System Interfaces
            </span>
            <span class="rounded-full border border-mint/25 bg-mint/10 px-2.5 py-0.5 font-mono text-[9px] font-semibold uppercase tracking-wider text-mint shadow-xs backdrop-blur-sm">
              Selected Pages &amp; Screens
            </span>
          </div>

          <!-- Web Admin & Certificate Showcase (Clean 2-Card Layout) -->
          <div class="space-y-3.5">
            <span class="font-mono text-[10px] font-bold uppercase tracking-widest text-muted">
              Web Admin &amp; Digital Certificate
            </span>
            <div class="grid min-w-0 gap-4 sm:grid-cols-2">
              <!-- Card 1: Web Admin Verification (Multi-Page Gallery with 3 Pages) -->
              <figure class="group flex min-w-0 flex-col overflow-hidden rounded-xl border border-slate-200/80 bg-slate-50/60 shadow-xs transition-all hover:border-mint/50 hover:shadow-md dark:border-white/10 dark:bg-white/[0.02] dark:hover:border-mint/40">
                <button
                  type="button"
                  class="image-preview-trigger relative block w-full overflow-hidden bg-[#e8e2d6] cursor-pointer focus:outline-none focus:ring-2 focus:ring-mint/60"
                  data-gallery="emedicard-admin"
                  data-image-src={emedicardAdminReviewImage}
                  data-image-alt="eMediCard web admin application review and document verification screen"
                  data-image-label="Web Admin — Application Review"
                  aria-label="Preview Web Admin Verification"
                >
                  <img
                    src={emedicardAdminReviewImage}
                    alt="eMediCard web admin document verification screen"
                    class="aspect-[16/10] w-full object-cover object-top transition-transform duration-300 group-hover:scale-[1.02]"
                    loading="lazy"
                  />
                  <div class="absolute inset-0 flex items-center justify-center bg-black/40 opacity-0 backdrop-blur-[1px] transition-opacity duration-200 group-hover:opacity-100">
                    <span class="inline-flex items-center gap-1.5 rounded-md border border-white/20 bg-black/70 px-2.5 py-1 text-[11px] font-semibold text-white shadow-md">
                      <Maximize2 size={12} />
                      <span>View Full Size (3 Pages)</span>
                    </span>
                  </div>
                  <!-- Multi-Page Indicator Badge -->
                  <div class="absolute bottom-2.5 right-2.5 rounded-md border border-white/15 bg-black/75 px-2 py-0.5 font-mono text-[9.5px] font-semibold text-mint shadow-md backdrop-blur-sm">
                    3 Pages
                  </div>
                </button>

                <!-- Hidden triggers for the other 2 admin views in the same gallery -->
                <button
                  type="button"
                  class="image-preview-trigger hidden"
                  data-gallery="emedicard-admin"
                  data-image-src={emedicardAdminReview2Image}
                  data-image-alt="eMediCard web admin laboratory requirements audit screen"
                  data-image-label="Web Admin — Application Review"
                  tabindex="-1"
                  aria-hidden="true"
                ></button>
                <button
                  type="button"
                  class="image-preview-trigger hidden"
                  data-gallery="emedicard-admin"
                  data-image-src={emedicardAdminApplicationListImage}
                  data-image-alt="eMediCard web admin applicant registry and queue list"
                  data-image-label="Web Admin — Application Queue"
                  tabindex="-1"
                  aria-hidden="true"
                ></button>

                <figcaption class="flex flex-col p-3 border-t border-slate-200/60 bg-white/60 dark:border-white/6 dark:bg-transparent">
                  <div class="flex items-center justify-between">
                    <span class="text-xs font-semibold text-ink">
                      Web Admin Verification
                    </span>
                    <span class="font-mono text-[10px] text-muted">
                      3 Pages
                    </span>
                  </div>
                  <span class="mt-0.5 text-[11px] text-muted leading-tight">
                    Staff review, applicant queue &amp; lab requirement audit
                  </span>
                </figcaption>
              </figure>

              <!-- Card 2: Digital Health Certificate -->
              <figure class="group flex min-w-0 flex-col overflow-hidden rounded-xl border border-slate-200/80 bg-slate-50/60 shadow-xs transition-all hover:border-mint/50 hover:shadow-md dark:border-white/10 dark:bg-white/[0.02] dark:hover:border-mint/40">
                <button
                  type="button"
                  class="image-preview-trigger relative block w-full overflow-hidden bg-[#fdfbf7] dark:bg-[#151b22] cursor-pointer focus:outline-none focus:ring-2 focus:ring-mint/60"
                  data-gallery="emedicard-certificate"
                  data-image-src={emedicardCertificateImage}
                  data-image-alt="eMediCard sanitized sample health certificate"
                  data-image-label="Digital Health Certificate"
                  aria-label="Preview Digital Health Certificate"
                >
                  <img
                    src={emedicardCertificateImage}
                    alt="eMediCard sanitized sample health certificate"
                    class="aspect-[16/10] w-full object-contain p-2 transition-transform duration-300 group-hover:scale-[1.02]"
                    loading="lazy"
                  />
                  <div class="absolute inset-0 flex items-center justify-center bg-black/40 opacity-0 backdrop-blur-[1px] transition-opacity duration-200 group-hover:opacity-100">
                    <span class="inline-flex items-center gap-1.5 rounded-md border border-white/20 bg-black/70 px-2.5 py-1 text-[11px] font-semibold text-white shadow-md">
                      <Maximize2 size={12} />
                      <span>View Full Size</span>
                    </span>
                  </div>
                </button>
                <figcaption class="flex flex-col p-3 border-t border-slate-200/60 bg-white/60 dark:border-white/6 dark:bg-transparent">
                  <span class="text-xs font-semibold text-ink">
                    Digital Health Certificate
                  </span>
                  <span class="mt-0.5 text-[11px] text-muted leading-tight">
                    Instant QR-verified compliance document
                  </span>
                </figcaption>
              </figure>
            </div>
          </div>

          <!-- Mobile App Workflow Reel (strictly contained) -->
          <div class="min-w-0 space-y-3">
            <span class="font-mono text-[10px] font-bold uppercase tracking-widest text-muted">
              Mobile Applicant &amp; Inspector Workflow
            </span>
            <div class="w-full max-w-full overflow-hidden rounded-xl border border-slate-200/80 bg-slate-50/50 p-3 dark:border-white/8 dark:bg-black/20">
              <div class="flex w-full gap-3 overflow-x-auto pb-1 scrollbar-thin scrollbar-thumb-slate-300 dark:scrollbar-thumb-white/10">
                {[
                  { src: emedicardCardTypeImage, alt: "eMediCard health card type selection screen", label: "Card Type" },
                  { src: emedicardDocumentRequirementsImage, alt: "eMediCard document requirements checklist screen", label: "Requirements" },
                  { src: emedicardUploadDocumentsImage, alt: "eMediCard required document upload screen", label: "Upload Docs" },
                  { src: emedicardReviewSubmitImage, alt: "eMediCard review and submit document summary screen", label: "Review" },
                  { src: emedicardPaymentMethodsImage, alt: "eMediCard payment methods screen", label: "Payment" },
                  { src: emedicardHealthCardImage, alt: "eMediCard digital health card QR screen", label: "Health Card" },
                  { src: emedicardInspectorScannerImage, alt: "eMediCard sanitary inspector QR scanner verification screen", label: "Inspector QR Scanner" },
                ].map((screen) => (
                  <figure class="w-28 shrink-0 sm:w-32 group">
                    <button
                      type="button"
                      class="image-preview-trigger relative block overflow-hidden rounded-xl border border-slate-200/80 bg-white transition hover:border-mint/50 hover:shadow-md focus:outline-none focus:ring-2 focus:ring-mint/60 dark:border-white/10 dark:bg-white/5 dark:hover:border-mint/40 cursor-pointer"
                      data-gallery="emedicard-mobile"
                      data-image-src={screen.src}
                      data-image-alt={screen.alt}
                      data-image-label={`Mobile Workflow — ${screen.label}`}
                      aria-label={`Preview ${screen.label} screen`}
                    >
                      <img
                        src={screen.src}
                        alt={screen.alt}
                        class="aspect-[9/19] w-full object-cover object-top transition-transform duration-300 group-hover:scale-[1.03]"
                        loading="lazy"
                      />
                      <div class="absolute inset-0 flex items-center justify-center bg-black/40 opacity-0 backdrop-blur-[1px] transition-opacity duration-200 group-hover:opacity-100">
                        <Maximize2 size={14} class="text-white" />
                      </div>
                    </button>
                    <figcaption class="mt-1.5 text-center font-mono text-[10px] font-semibold text-muted">
                      {screen.label}
                    </figcaption>
                  </figure>
                ))}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Readorama Case Study Modal (High-End Clean Showcase) -->
  <div
    id="readorama-modal"
    class="project-modal fixed inset-0 z-[100] hidden items-center justify-center bg-black/80 p-4 backdrop-blur-md sm:p-6"
    role="dialog"
    aria-modal="true"
    aria-labelledby="readorama-modal-title"
  >
    <div class="project-modal-panel max-h-[90vh] w-full max-w-5xl overflow-y-auto overflow-x-hidden rounded-2xl border border-slate-200/90 bg-white text-slate-950 shadow-2xl transition-all dark:border-white/10 dark:bg-[#0c1015] dark:text-ink">
      <!-- Modal Header -->
      <div class="project-modal-header sticky top-0 z-20 flex flex-col gap-3 border-b border-slate-200/80 bg-white/95 px-6 py-4.5 backdrop-blur-md dark:border-white/8 dark:bg-[#0c1015]/95 sm:flex-row sm:items-center sm:justify-between">
        <div class="space-y-1">
          <div class="flex items-center gap-2">
            <h2 id="readorama-modal-title" class="font-display text-2xl font-bold tracking-tight text-ink">
              Readorama
            </h2>
            <span class="rounded-md border border-mint/30 bg-mint/10 px-2 py-0.5 font-mono text-[10px] font-semibold text-mint">
              2023 • Figma Design
            </span>
          </div>
          <p class="text-xs text-muted">
            Academic Group Project • Mobile Book Exchange &amp; Marketplace
          </p>
        </div>
        <div class="flex items-center gap-2.5">
          <a
            href="https://www.figma.com/design/9YsrKmz6fTSyGthAlcwRA6/Group-5?node-id=0-1&p=f&t=oSqlp4PKdApFvZAr-0"
            target="_blank"
            rel="noreferrer"
            class="inline-flex h-8 items-center justify-center gap-1.5 rounded-lg bg-mint px-3 text-xs font-bold text-[#07100d] shadow-sm transition-all hover:bg-mint/90 hover:shadow focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-mint"
          >
            <span>Open in Figma</span>
            <ArrowUpRight size={13} />
          </a>
          <button
            type="button"
            class="project-modal-close inline-flex size-8 items-center justify-center rounded-lg border border-transparent text-muted transition-colors hover:border-slate-200 hover:bg-slate-100 hover:text-ink dark:hover:border-white/10 dark:hover:bg-white/10 dark:hover:text-ink cursor-pointer"
            aria-label="Close Readorama details"
          >
            <X size={16} />
          </button>
        </div>
      </div>

      <!-- Modal Body Grid -->
      <div class="grid min-w-0 gap-6 p-6 lg:grid-cols-[minmax(0,0.85fr)_minmax(0,1.15fr)] lg:gap-8">
        <!-- Left Column: Scope & Overview (Typography-First) -->
        <div class="min-w-0 flex flex-col justify-between space-y-6">
          <div class="space-y-6">
            <!-- Individual Contributions -->
            <div class="space-y-3.5">
              <span class="font-mono text-[10px] font-bold uppercase tracking-widest text-mint">
                Role &amp; Individual Contributions
              </span>
              <ul class="space-y-4">
                <li class="space-y-1">
                  <div class="flex items-center gap-2">
                    <span class="size-1.5 shrink-0 rounded-full bg-mint"></span>
                    <h3 class="font-sans text-sm font-semibold text-ink">Sell Books Listing Workflow</h3>
                  </div>
                  <p class="pl-3.5 text-xs sm:text-sm text-muted leading-relaxed">
                    Inspired by MyAnimeList’s catalog and metadata architecture — designed a structured mobile listing flow featuring cover art uploads, edition metadata, condition grading, and pricing controls optimized for quick mobile browsing.
                  </p>
                </li>

                <li class="space-y-1">
                  <div class="flex items-center gap-2">
                    <span class="size-1.5 shrink-0 rounded-full bg-mint"></span>
                    <h3 class="font-sans text-sm font-semibold text-ink">Community Forum &amp; P2P Trading Chat</h3>
                  </div>
                  <p class="pl-3.5 text-xs sm:text-sm text-muted leading-relaxed">
                    Designed community forum boards and direct chat interfaces enabling readers to connect, discuss titles, and arrange peer-to-peer book trades.
                  </p>
                </li>
              </ul>
            </div>

            <!-- Subtle Divider -->
            <div class="border-t border-slate-200/70 dark:border-white/8"></div>

            <!-- Project Concept -->
            <div class="space-y-2">
              <span class="font-mono text-[10px] font-bold uppercase tracking-widest text-muted">
                About the Project
              </span>
              <p class="text-xs sm:text-sm text-muted leading-relaxed">
                Readorama is a mobile book marketplace designed to connect readers, sellers, and collectors — enabling users to buy, sell, or trade new and used books at affordable rates.
              </p>
            </div>
          </div>

          <!-- Timeline & Tools Footer -->
          <div class="flex flex-wrap items-center justify-between gap-2 border-t border-slate-200/70 pt-4 dark:border-white/8 text-xs text-muted">
            <div class="flex items-center gap-2">
              <span class="font-mono text-[10px] font-bold uppercase tracking-wider text-muted">Timeline:</span>
              <span>2023 (Academic Project)</span>
            </div>
            <div class="flex items-center gap-2">
              <span class="font-mono text-[10px] font-bold uppercase tracking-wider text-muted">Tools:</span>
              <span>Figma • Wireframing • Prototyping</span>
            </div>
          </div>
        </div>

        <!-- Right Column: Design Artifacts Showcase -->
        <div class="space-y-3.5">
          <span class="font-mono text-[10px] font-bold uppercase tracking-widest text-muted">
            Design Artifacts
          </span>

          <div class="space-y-4">
            {[
              {
                src: readoramaBookSellingImage,
                alt: "Readorama book selling and listing interface workflow",
                title: "Book Selling & Listing Flow",
                subtitle: "MyAnimeList-inspired catalog & listing screens",
              },
              {
                src: readoramaDesignSystemImage,
                alt: "Readorama brand guidelines and design system overview",
                title: "Brand Guidelines & System",
                subtitle: "Typography scale, palette & components",
              },
            ].map((preview) => (
              <figure class="group flex flex-col overflow-hidden rounded-xl border border-slate-200/80 bg-slate-50/60 shadow-xs transition-all hover:border-mint/50 hover:shadow-md dark:border-white/10 dark:bg-white/[0.02] dark:hover:border-mint/40">
                <button
                  type="button"
                  class="image-preview-trigger relative block w-full overflow-hidden bg-bg-deep cursor-pointer focus:outline-none focus:ring-2 focus:ring-mint/60"
                  data-gallery="readorama"
                  data-image-src={preview.src}
                  data-image-alt={preview.alt}
                  data-image-label={preview.title}
                  aria-label={`Preview ${preview.title}`}
                >
                  <img
                    src={preview.src}
                    alt={preview.alt}
                    class="aspect-[16/10] w-full object-cover object-top transition-transform duration-300 group-hover:scale-[1.02]"
                    loading="lazy"
                  />
                  <div class="absolute inset-0 flex items-center justify-center bg-black/40 opacity-0 backdrop-blur-[1px] transition-opacity duration-200 group-hover:opacity-100">
                    <span class="inline-flex items-center gap-1.5 rounded-md border border-white/20 bg-black/70 px-2.5 py-1 text-[11px] font-semibold text-white shadow-md">
                      <Maximize2 size={12} />
                      <span>View Full Size</span>
                    </span>
                  </div>
                </button>
                <figcaption class="flex flex-col sm:flex-row sm:items-center sm:justify-between p-3.5 border-t border-slate-200/60 bg-white/60 dark:border-white/6 dark:bg-transparent gap-1">
                  <div>
                    <p class="text-xs font-semibold text-ink">
                      {preview.title}
                    </p>
                    <p class="text-[11px] text-muted">
                      {preview.subtitle}
                    </p>
                  </div>
                </figcaption>
              </figure>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Image Preview Fullscreen Modal (shadcn style with gallery carousel navigation) -->
  <div
    id="image-preview-modal"
    class="fixed inset-0 z-[120] hidden items-center justify-center bg-black/90 p-4 backdrop-blur-md sm:p-6"
    role="dialog"
    aria-modal="true"
    aria-labelledby="image-preview-title"
  >
    <div class="relative flex max-h-[94vh] w-full max-w-5xl flex-col items-center">
      <!-- Close Button -->
      <button
        type="button"
        id="image-preview-close"
        class="absolute -top-12 right-0 z-30 inline-flex items-center gap-1.5 rounded-md border border-white/20 bg-black/70 px-3 py-1.5 text-xs font-semibold text-white shadow-lg backdrop-blur-md transition-all hover:bg-white/20 hover:border-white/40 focus:outline-none focus:ring-2 focus:ring-mint/60 cursor-pointer"
        aria-label="Close image preview"
      >
        <X size={14} />
        <span>Close</span>
      </button>

      <!-- Previous Image Navigation Button -->
      <button
        type="button"
        id="image-preview-prev"
        class="absolute left-2 sm:-left-14 top-1/2 -translate-y-1/2 z-30 inline-flex size-10 sm:size-12 items-center justify-center rounded-full border border-white/20 bg-black/75 text-white shadow-xl backdrop-blur-md transition hover:bg-mint hover:text-[#07100d] hover:border-mint focus:outline-none focus:ring-2 focus:ring-mint/60 cursor-pointer"
        aria-label="Previous image"
      >
        <ChevronLeft size={22} />
      </button>

      <!-- Next Image Navigation Button -->
      <button
        type="button"
        id="image-preview-next"
        class="absolute right-2 sm:-right-14 top-1/2 -translate-y-1/2 z-30 inline-flex size-10 sm:size-12 items-center justify-center rounded-full border border-white/20 bg-black/75 text-white shadow-xl backdrop-blur-md transition hover:bg-mint hover:text-[#07100d] hover:border-mint focus:outline-none focus:ring-2 focus:ring-mint/60 cursor-pointer"
        aria-label="Next image"
      >
        <ChevronRight size={22} />
      </button>

      <!-- Image Box Container -->
      <div class="max-h-[82vh] overflow-auto rounded-xl border border-white/15 bg-[#080d11] p-3 shadow-2xl">
        <img
          id="image-preview-img"
          src=""
          alt=""
          class="mx-auto block max-h-[75vh] min-w-[18rem] max-w-full rounded-lg object-contain transition-opacity duration-200"
        />
      </div>

      <!-- Title & Counter Footer -->
      <div class="mt-3.5 flex flex-wrap items-center justify-center gap-2">
        <p id="image-preview-title" class="rounded-full border border-white/15 bg-black/70 px-3.5 py-1 text-center font-mono text-xs font-semibold uppercase tracking-wider text-white/90 shadow-md backdrop-blur-md"></p>
        <span id="image-preview-counter" class="rounded-full border border-mint/30 bg-mint/10 px-2.5 py-1 text-center font-mono text-[11px] font-semibold text-mint shadow-md backdrop-blur-md"></span>
      </div>
    </div>
  </div>

  <footer class="border-t border-white/8 bg-bg-deep/70 py-7">
    <div class="page-shell flex flex-col justify-between gap-3 text-xs text-muted sm:flex-row sm:items-center">
      <p>&copy; 2026 Sean Paul S. Lapasanda. All rights reserved.</p>
      <div class="flex flex-wrap gap-x-4 gap-y-2 font-bold">
        {profileLinks.map((link) => (
          <a
            href={link.href}
            id={`${link.id}-footer`}
            target={link.href.startsWith("mailto:") ? undefined : "_blank"}
            rel={link.href.startsWith("mailto:") ? undefined : "noreferrer"}
            class="transition hover:text-ink"
          >
            {link.label}
          </a>
        ))}
        <a href="#top" class="transition hover:text-ink">Back to top</a>
      </div>
    </div>
  </footer>
</MainLayout>

<!-- Client scripts for scroll animations and form submission simulation -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    // 0. Theme toggle
    const themeToggle = document.getElementById("theme-toggle");
    const storageKey = "ichihara-theme";

    function setTheme(theme: "light" | "dark") {
      document.documentElement.dataset.theme = theme;
      document.documentElement.classList.toggle("dark", theme === "dark");
      themeToggle?.setAttribute("aria-pressed", String(theme === "light"));
      themeToggle?.setAttribute("aria-label", theme === "light" ? "Switch to dark mode" : "Switch to light mode");

      try {
        window.localStorage.setItem(storageKey, theme);
      } catch {
        // Storage may be disabled; the visual toggle should still work for this page view.
      }
    }

    themeToggle?.addEventListener("click", () => {
      const currentTheme = document.documentElement.dataset.theme === "light" ? "light" : "dark";
      setTheme(currentTheme === "light" ? "dark" : "light");
    });

    const initialTheme = document.documentElement.dataset.theme === "light" ? "light" : "dark";
    document.documentElement.classList.toggle("dark", initialTheme === "dark");
    themeToggle?.setAttribute("aria-pressed", String(initialTheme === "light"));
    themeToggle?.setAttribute("aria-label", initialTheme === "light" ? "Switch to dark mode" : "Switch to light mode");

    // 1. Intersection Observer for Scroll entry animations
    const elements = document.querySelectorAll(".scroll-reveal");
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add("show");
          observer.unobserve(entry.target); // Animate once
        }
      });
    }, {
      threshold: 0.05,
      rootMargin: "0px 0px -50px 0px"
    });

    elements.forEach(el => observer.observe(el));

    // 2. Mobile navigation disclosure
    const mobileMenuToggle = document.getElementById("mobile-menu-toggle");
    const mobileNavPanel = document.getElementById("mobile-nav-panel");
    const mobileMenuLabel = document.getElementById("mobile-menu-label");
    const mobileNavLinks = document.querySelectorAll(".mobile-nav-link");

    function setMobileMenu(open: boolean) {
      if (!mobileMenuToggle || !mobileNavPanel || !mobileMenuLabel) return;

      mobileMenuToggle.setAttribute("aria-expanded", String(open));
      mobileMenuLabel.textContent = open ? "Close" : "Menu";
      mobileNavPanel.classList.toggle("hidden", !open);
    }

    mobileMenuToggle?.addEventListener("click", () => {
      const isOpen = mobileMenuToggle.getAttribute("aria-expanded") === "true";
      setMobileMenu(!isOpen);
    });

    mobileNavLinks.forEach((link) => {
      link.addEventListener("click", () => setMobileMenu(false));
    });

    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape") setMobileMenu(false);
    });

    // 3. Project detail modals & Image preview lightbox with Gallery Navigation
    const modalTriggers = document.querySelectorAll<HTMLElement>(".project-modal-trigger");
    const modals = document.querySelectorAll<HTMLElement>(".project-modal");
    const imagePreviewModal = document.getElementById("image-preview-modal");
    const imagePreviewImg = document.getElementById("image-preview-img") as HTMLImageElement | null;
    const imagePreviewTitle = document.getElementById("image-preview-title");
    const imagePreviewCounter = document.getElementById("image-preview-counter");
    const imagePreviewClose = document.getElementById("image-preview-close");
    const imagePreviewPrev = document.getElementById("image-preview-prev");
    const imagePreviewNext = document.getElementById("image-preview-next");
    const imagePreviewTriggers = document.querySelectorAll<HTMLElement>(".image-preview-trigger");

    let activeModalTrigger: HTMLElement | null = null;
    let currentGalleryList: HTMLElement[] = [];
    let currentGalleryIndex = 0;

    function closeProjectModal() {
      modals.forEach((modal) => {
        modal.classList.add("hidden");
        modal.classList.remove("flex");
      });
      document.body.style.overflow = "";
      activeModalTrigger?.focus();
      activeModalTrigger = null;
    }

    function openProjectModal(modalId: string, trigger: HTMLElement) {
      const modal = document.getElementById(modalId);
      if (!modal) return;

      activeModalTrigger = trigger;
      modal.classList.remove("hidden");
      modal.classList.add("flex");
      document.body.style.overflow = "hidden";
      modal.querySelector<HTMLElement>(".project-modal-close")?.focus();
    }

    function updateImagePreviewDisplay() {
      if (!imagePreviewImg || !imagePreviewTitle || currentGalleryList.length === 0) return;
      const currentTrigger = currentGalleryList[currentGalleryIndex];
      if (!currentTrigger) return;

      const src = currentTrigger.dataset.imageSrc || "";
      const alt = currentTrigger.dataset.imageAlt || "Project screen preview";
      const label = currentTrigger.dataset.imageLabel || "";

      imagePreviewImg.style.opacity = "0.35";
      imagePreviewImg.src = src;
      imagePreviewImg.alt = alt;
      imagePreviewTitle.textContent = label;

      if (imagePreviewCounter) {
        if (currentGalleryList.length > 1) {
          imagePreviewCounter.textContent = `${currentGalleryIndex + 1} / ${currentGalleryList.length}`;
          imagePreviewCounter.classList.remove("hidden");
        } else {
          imagePreviewCounter.classList.add("hidden");
        }
      }

      setTimeout(() => {
        if (imagePreviewImg) imagePreviewImg.style.opacity = "1";
      }, 50);

      // Hide or show prev/next buttons if only 1 item
      if (imagePreviewPrev && imagePreviewNext) {
        const hasMultiple = currentGalleryList.length > 1;
        imagePreviewPrev.style.display = hasMultiple ? "inline-flex" : "none";
        imagePreviewNext.style.display = hasMultiple ? "inline-flex" : "none";
      }
    }

    function showNextImage() {
      if (currentGalleryList.length <= 1) return;
      currentGalleryIndex = (currentGalleryIndex + 1) % currentGalleryList.length;
      updateImagePreviewDisplay();
    }

    function showPrevImage() {
      if (currentGalleryList.length <= 1) return;
      currentGalleryIndex = (currentGalleryIndex - 1 + currentGalleryList.length) % currentGalleryList.length;
      updateImagePreviewDisplay();
    }

    function openImagePreview(trigger: HTMLElement) {
      if (!imagePreviewModal) return;

      const galleryName = trigger.dataset.gallery;
      if (galleryName) {
        currentGalleryList = Array.from(document.querySelectorAll<HTMLElement>(`.image-preview-trigger[data-gallery="${galleryName}"]`));
      } else {
        const parentModal = trigger.closest(".project-modal") || trigger.closest("section");
        currentGalleryList = parentModal 
          ? Array.from(parentModal.querySelectorAll<HTMLElement>(".image-preview-trigger"))
          : [trigger];
      }

      currentGalleryIndex = currentGalleryList.indexOf(trigger);
      if (currentGalleryIndex === -1) currentGalleryIndex = 0;

      updateImagePreviewDisplay();
      imagePreviewModal.classList.remove("hidden");
      imagePreviewModal.classList.add("flex");
      imagePreviewClose?.focus();
    }

    function closeImagePreview() {
      imagePreviewModal?.classList.add("hidden");
      imagePreviewModal?.classList.remove("flex");
      if (currentGalleryList[currentGalleryIndex]) {
        currentGalleryList[currentGalleryIndex].focus();
      }
      if (imagePreviewImg) {
        imagePreviewImg.src = "";
        imagePreviewImg.alt = "";
      }
      if (imagePreviewTitle) imagePreviewTitle.textContent = "";
      if (imagePreviewCounter) imagePreviewCounter.textContent = "";
    }

    modalTriggers.forEach((trigger) => {
      trigger.addEventListener("click", () => {
        const modalId = trigger.dataset.projectModalTarget;
        if (modalId) openProjectModal(modalId, trigger);
      });
    });

    modals.forEach((modal) => {
      modal.addEventListener("click", (event) => {
        if (event.target === modal) closeProjectModal();
      });

      modal.querySelectorAll(".project-modal-close").forEach((button) => {
        button.addEventListener("click", closeProjectModal);
      });
    });

    imagePreviewTriggers.forEach((trigger) => {
      trigger.addEventListener("click", () => openImagePreview(trigger));
    });

    imagePreviewModal?.addEventListener("click", (event) => {
      if (event.target === imagePreviewModal) closeImagePreview();
    });

    imagePreviewClose?.addEventListener("click", closeImagePreview);

    imagePreviewPrev?.addEventListener("click", (e) => {
      e.stopPropagation();
      showPrevImage();
    });

    imagePreviewNext?.addEventListener("click", (e) => {
      e.stopPropagation();
      showNextImage();
    });

    document.addEventListener("keydown", (event) => {
      if (imagePreviewModal?.classList.contains("flex")) {
        if (event.key === "Escape") {
          closeImagePreview();
        } else if (event.key === "ArrowRight") {
          showNextImage();
        } else if (event.key === "ArrowLeft") {
          showPrevImage();
        }
        return;
      }
      if (event.key === "Escape") {
        closeProjectModal();
      }
    });

  });
</script>
