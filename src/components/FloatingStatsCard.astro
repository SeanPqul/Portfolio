---
import { Globe, Smartphone, Code } from "@lucide/astro";
import profileImage from "../assets/profile.jpg?url";
import profileImageTwo from "../assets/profile-2.jpg?url";
import profileImageThree from "../assets/profile-3.jpg?url";
import profileImageFour from "../assets/profile-4.jpg?url";

const profileImages = [
  { src: profileImage, pos: "16% 25%", zoom: "scale(1.7) translate(14%, 8%)" },
  { src: profileImageTwo, pos: "16% 25%", zoom: "scale(1.7) translate(14%, 8%)" },
  { src: profileImageThree, pos: "68% 15%", zoom: "scale(1.65) translate(-14%, 6%)" },
  { src: profileImageFour, pos: "50% 15%", zoom: "scale(1.6) translate(0%, 6%)" },
];
---

<div class="relative mx-auto flex w-full max-w-[460px] flex-col items-center justify-center overflow-visible px-2 py-2 sm:px-4 lg:max-w-none">
  
  <!-- Backlight Glow Blobs -->
  <div class="absolute top-1/2 left-1/3 -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-mint/5 rounded-full blur-3xl pointer-events-none animate-pulse-glow"></div>
  <div class="absolute top-1/2 right-1/3 translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-sky/5 rounded-full blur-3xl pointer-events-none animate-pulse-glow" style="animation-delay: 2s;"></div>

  <!-- Central Camera Viewfinder Deck Container -->
  <div class="relative z-10 flex w-[min(76vw,280px)] flex-col items-center select-none sm:w-[320px] md:w-[340px] lg:w-[335px] xl:w-[365px]">
    
    <!-- Camera Viewfinder Screen -->
    <div 
      id="profile-card-container"
      data-profile-images={JSON.stringify(profileImages)}
      class="relative w-full aspect-[4/5] sm:aspect-[3/4] rounded-2xl border border-white/12 bg-black overflow-hidden shadow-2xl group/photo transition-all duration-500 hover:border-mint/30 hover:shadow-mint/5 cursor-pointer"
      title="Click to cycle photoshoot images"
      role="button"
      tabindex="0"
      aria-label="Focus portrait"
    >
      <!-- Shutter flash overlay -->
      <div id="deck-shutter-flash" class="absolute inset-0 bg-white opacity-0 z-40 pointer-events-none transition-opacity duration-75"></div>

      <!-- Grain texture overlay -->
      <div class="absolute inset-0 grain opacity-8 pointer-events-none z-20"></div>

      <!-- Rule of Thirds Gridlines -->
      <div id="grid-lines" class="absolute inset-0 grid grid-cols-3 grid-rows-3 pointer-events-none z-10 opacity-15 transition-opacity duration-700">
        <div class="border-r border-b border-dashed border-white/30"></div>
        <div class="border-r border-b border-dashed border-white/30"></div>
        <div class="border-b border-dashed border-white/30"></div>
        <div class="border-r border-b border-dashed border-white/30"></div>
        <div class="border-r border-b border-dashed border-white/30"></div>
        <div class="border-b border-dashed border-white/30"></div>
        <div class="border-r border-dashed border-white/30"></div>
        <div class="border-r border-dashed border-white/30"></div>
        <div></div>
      </div>

      <!-- Viewfinder Overlay Info (RAW/4K, Autofocus Brackets) -->
      <div class="camera-hud-overlay absolute inset-0 p-3.5 sm:p-4 z-20 font-mono text-[8px] sm:text-[9px] font-bold pointer-events-none tracking-wider">
        <!-- Top Row -->
        <div class="flex items-center justify-end w-full">
          <div class="camera-hud-badge flex items-center gap-1 px-1.5 py-0.5 rounded text-[7.5px] sm:text-[8.5px]">
            <span>RAW</span>
            <span class="text-sky font-normal">4K 60</span>
          </div>
        </div>

        <!-- Center Autofocus Brackets -->
        <div class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 flex items-center justify-center">
          <div 
            id="af-box" 
            class="relative w-14 h-14 sm:w-16 sm:h-16 border border-white/30 rounded transition-all duration-300 ease-out scale-100 flex items-center justify-center"
          >
            <!-- Center AF dot -->
            <div id="af-dot" class="w-1.5 h-1.5 bg-white/40 rounded-full transition-colors"></div>
            <!-- Technical AF corner accents -->
            <span class="absolute -top-[1px] -left-[1px] w-3 h-3 sm:w-3.5 sm:h-3.5 border-t border-l border-amber transition-colors" id="af-c1"></span>
            <span class="absolute -top-[1px] -right-[1px] w-3 h-3 sm:w-3.5 sm:h-3.5 border-t border-r border-amber transition-colors" id="af-c2"></span>
            <span class="absolute -bottom-[1px] -left-[1px] w-3 h-3 sm:w-3.5 sm:h-3.5 border-b border-l border-amber transition-colors" id="af-c3"></span>
            <span class="absolute -bottom-[1px] -right-[1px] w-3 h-3 sm:w-3.5 sm:h-3.5 border-b border-r border-amber transition-colors" id="af-c4"></span>
          </div>
          
          <div id="hud-focus-status" class="absolute top-10 text-[7px] sm:text-[8px] bg-amber/10 border border-amber/20 text-amber px-1.5 py-0.5 rounded opacity-0 transition-opacity duration-300 whitespace-nowrap">
            AF-S LOCK
          </div>
        </div>
      </div>

      <!-- Bottom DSLR Viewfinder Status Bar (Full width, no inset padding, solid background matching real DSLR viewfinders) -->
      <div class="dslr-hud absolute bottom-0 left-0 right-0 z-20 flex min-h-[56px] sm:min-h-[60px] flex-col justify-center gap-1.5 rounded-b-2xl px-4 py-2.5 font-mono text-[8.5px] sm:text-[9.5px] font-bold tracking-wider pointer-events-none">
        <!-- Exposure Scale -->
        <div class="dslr-exposure-scale flex justify-center items-center gap-2 text-[8.5px] sm:text-[9.5px]">
          <span>-2</span>
          <span>·</span>
          <span>-1</span>
          <span>·</span>
          <span class="dslr-exposure-zero">0</span>
          <span>·</span>
          <span>+1</span>
          <span>·</span>
          <span>+2</span>
        </div>
        
        <div class="dslr-readout-row flex items-center justify-between gap-2 text-[8.5px] sm:text-[9.5px]">
          <div class="dslr-camera-values flex items-center gap-3">
            <span><span class="dslr-label">SS:</span> <span class="dslr-value">1/125</span></span>
            <span><span class="dslr-label">F:</span> <span class="dslr-value">2.8</span></span>
            <span><span class="dslr-label">ISO:</span> <span class="dslr-value">400</span></span>
          </div>
          <div class="dslr-battery-group flex items-center gap-1.5">
            <span class="dslr-label">BATT</span>
            <div class="dslr-battery relative flex h-3.5 w-6.5 items-center rounded-2xs p-0.2">
              <div class="h-full w-4/5 bg-mint rounded-3xs"></div>
            </div>
            <span class="dslr-label">88%</span>
          </div>
        </div>
      </div>
      
      <!-- Image -->
      <img 
        id="profile-card-image"
        src={profileImage}
        alt="Sean Paul Lapasanda portrait" 
        class="w-full h-full object-cover object-[16%_25%] transition-all duration-700 ease-out"
        style="transform: scale(1); filter: blur(2px) contrast(0.95);"
      />
    </div>
  </div>

  <!-- FLOATING BADGES (Preserving Orbiting Layer Depth) -->
  <!-- FLOATING BADGE 1: Web Systems (Top Right) -->
  <div class="hero-float-badge hero-float-badge-web pointer-events-auto absolute right-0 top-2 z-20 animate-float sm:-right-4 sm:top-1 lg:-right-6 lg:top-3 xl:-right-8 xl:top-2">
    <article class="glass w-[112px] rounded-xl border border-white/8 p-2.5 text-left transition-all duration-300 hover:scale-105 hover:border-sky/30 hover:bg-sky/5 hover:shadow-lg hover:shadow-sky/5 sm:w-[140px] sm:p-3 lg:w-[130px] xl:w-[145px]">
      <div class="flex items-center gap-1.5 mb-1">
        <span class="grid size-5 place-items-center rounded bg-sky/10 border border-sky/20 text-sky">
          <Globe size={11} />
        </span>
        <span class="text-[8px] font-mono font-bold text-sky uppercase tracking-wider">Solutions</span>
      </div>
      <p class="text-xs font-extrabold font-display text-ink leading-tight">Web Systems</p>
      <p class="text-[7px] text-muted leading-tight mt-0.5">Dashboards & workflows</p>
    </article>
  </div>

  <!-- FLOATING BADGE 2: Mobile Apps (Middle Left) -->
  <div class="hero-float-badge hero-float-badge-mobile pointer-events-auto absolute left-0 top-1/3 z-20 animate-float-delayed sm:-left-4 lg:-left-6 lg:top-[36%] xl:-left-8">
    <article class="glass w-[112px] rounded-xl border border-white/8 p-2.5 text-left transition-all duration-300 hover:scale-105 hover:border-mint/30 hover:bg-mint/5 hover:shadow-lg hover:shadow-mint/5 sm:w-[140px] sm:p-3 lg:w-[130px] xl:w-[145px]">
      <div class="flex items-center gap-1.5 mb-1">
        <span class="grid size-5 place-items-center rounded bg-mint/10 border border-mint/20 text-mint">
          <Smartphone size={11} />
        </span>
        <span class="text-[8px] font-mono font-bold text-mint uppercase tracking-wider">Mobile</span>
      </div>
      <p class="text-xs font-extrabold font-display text-ink leading-tight">Mobile Apps</p>
      <p class="text-[7px] text-muted leading-tight mt-0.5">Cross-platform products</p>
    </article>
  </div>

  <!-- FLOATING BADGE 3: Tech Stack (Bottom Right) -->
  <div class="hero-float-badge hero-float-badge-stack pointer-events-auto absolute bottom-6 right-0 z-20 animate-float sm:-right-4 sm:bottom-5 lg:-right-6 lg:bottom-2 xl:-right-8 xl:bottom-1" style="animation-delay: 1.5s;">
    <article class="glass w-[112px] rounded-xl border border-white/8 p-2.5 text-left transition-all duration-300 hover:scale-105 hover:border-amber/30 hover:bg-amber/5 hover:shadow-lg hover:shadow-amber/5 sm:w-[140px] sm:p-3 lg:w-[130px] xl:w-[145px]">
      <div class="flex items-center gap-1.5 mb-1">
        <span class="grid size-5 place-items-center rounded bg-amber/10 border border-amber/20 text-amber">
          <Code size={11} />
        </span>
        <span class="text-[8px] font-mono font-bold text-amber uppercase tracking-wider">Tech Stack</span>
      </div>
      <p class="text-xs font-extrabold font-display text-ink leading-tight">Laravel & React</p>
      <p class="text-[7px] text-muted leading-tight mt-0.5">APIs · data · UI systems</p>
    </article>
  </div>

</div>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const container = document.getElementById("profile-card-container");
    const img = document.getElementById("profile-card-image") as HTMLImageElement;
    const flash = document.getElementById("deck-shutter-flash");
    const gridLines = document.getElementById("grid-lines");
    const hudFocusStatus = document.getElementById("hud-focus-status");
    
    const afBox = document.getElementById("af-box");
    const afDot = document.getElementById("af-dot");
    const afCornerElements = [
      document.getElementById("af-c1"),
      document.getElementById("af-c2"),
      document.getElementById("af-c3"),
      document.getElementById("af-c4")
    ];

    if (!container || !img) return;

    // Photoshoot images with coordinates & specific zoom targets for focus modes
    const images = JSON.parse(container.dataset.profileImages || "[]");

    // Preload all images into the browser cache immediately so there's no
    // delay or flash when cycling through them on click/hover.
    images.forEach((entry: { src: string }) => {
      const preloadImg = new Image();
      preloadImg.src = entry.src;
    });

    let currentIndex = 0;
    let isTransitioning = false;
    let isFocused = false;
    let autoFocusTimer: ReturnType<typeof setTimeout> | null = null;
    const touchQuery = window.matchMedia("(hover: none), (pointer: coarse)");

    function usesTouchFlow() {
      return touchQuery.matches;
    }

    function updateInteractionLabel() {
      if (!container) return;

      if (usesTouchFlow()) {
        container.setAttribute("aria-label", isFocused ? "Take next photo" : "Focus portrait");
        return;
      }

      container.setAttribute("aria-label", "Cycle photoshoot images");
    }

    // A. Apply focus or blur style dynamically
    function applyFocusStyles() {
      if (isFocused) {
        img.style.transform = images[currentIndex].zoom;
        img.style.filter = "blur(0px) contrast(1.05) saturate(1.02)";
      } else {
        img.style.transform = "scale(1) translate(0, 0)";
        img.style.filter = "blur(2px) contrast(0.95)";
      }
    }

    // B. AF Trigger
    function toggleFocus(forceState?: boolean) {
      isFocused = forceState !== undefined ? forceState : !isFocused;

      if (autoFocusTimer) clearTimeout(autoFocusTimer);

      if (isFocused) {
        if (afBox) {
          afBox.classList.remove("scale-100");
          afBox.classList.add("scale-115");
          afBox.style.borderColor = "rgba(246, 197, 111, 0.6)";
        }
        afCornerElements.forEach(el => {
          if (el) el.style.borderColor = "var(--color-amber)";
        });

        autoFocusTimer = setTimeout(() => {
          applyFocusStyles();
          
          if (afBox) {
            afBox.classList.remove("scale-115");
            afBox.classList.add("scale-95");
            afBox.style.borderColor = "rgba(102, 224, 181, 0.4)";
          }
          if (afDot) afDot.style.backgroundColor = "var(--color-mint)";
          afCornerElements.forEach(el => {
            if (el) el.style.borderColor = "var(--color-mint)";
          });
          if (gridLines) gridLines.style.opacity = "0.03";
          if (hudFocusStatus) {
            hudFocusStatus.style.opacity = "1";
            hudFocusStatus.classList.remove("text-amber", "bg-amber/10", "border-amber/20");
            hudFocusStatus.classList.add("text-mint", "bg-mint/10", "border-mint/20");
            hudFocusStatus.textContent = "AF-S LOCKED";
          }
        }, 400);

      } else {
        if (autoFocusTimer) clearTimeout(autoFocusTimer);
        applyFocusStyles();
        
        if (afBox) {
          afBox.classList.remove("scale-95", "scale-115");
          afBox.classList.add("scale-100");
          afBox.style.borderColor = "rgba(255, 255, 255, 0.3)";
        }
        if (afDot) afDot.style.backgroundColor = "rgba(255, 255, 255, 0.4)";
        afCornerElements.forEach(el => {
          if (el) el.style.borderColor = "var(--color-amber)";
        });
        if (gridLines) gridLines.style.opacity = "0.15";
        if (hudFocusStatus) hudFocusStatus.style.opacity = "0";
      }

      updateInteractionLabel();
    }

    function shootNextPhoto({ resetFocusAfterShot = false } = {}) {
      if (isTransitioning) return;
      isTransitioning = true;

      // Shutter flash effect
      if (flash) {
        flash.style.opacity = "1";
        setTimeout(() => {
          flash.style.opacity = "0";
        }, 80);
      }

      // Temporarily disable transition to prevent sliding/panning during swap
      img.style.transition = "none";
      img.style.opacity = "0.1";

      setTimeout(() => {
        currentIndex = (currentIndex + 1) % images.length;
        img.src = images[currentIndex].src;
        img.style.objectPosition = images[currentIndex].pos;

        if (resetFocusAfterShot) {
          toggleFocus(false);
        } else {
          applyFocusStyles();
        }

        img.onload = () => {
          // Re-enable transition before fading in so it is smooth
          img.style.transition = "";
          // Force layout reflow to register the transition change
          void img.offsetHeight;
          img.style.opacity = "1";
          isTransitioning = false;
          updateInteractionLabel();
        };

        setTimeout(() => {
          if (isTransitioning) {
            img.style.transition = "";
            void img.offsetHeight;
            img.style.opacity = "1";
            isTransitioning = false;
            updateInteractionLabel();
          }
        }, 150);

      }, 150);
    }

    // C. Click to focus on touch, and shutter on the next tap.
    container.addEventListener("click", () => {
      if (isTransitioning) return;

      if (usesTouchFlow()) {
        if (!isFocused) {
          toggleFocus(true);
          return;
        }

        shootNextPhoto({ resetFocusAfterShot: true });
        return;
      }

      shootNextPhoto();
    });

    // D. Hover Auto-focus Trigger
    container.addEventListener("mouseenter", () => {
      if (usesTouchFlow()) return;
      if (!isFocused) toggleFocus(true);
    });

    container.addEventListener("mouseleave", () => {
      if (usesTouchFlow()) return;
      toggleFocus(false);
    });

    container.addEventListener("keydown", (event) => {
      if (event.key !== "Enter" && event.key !== " ") return;

      event.preventDefault();
      if (!isFocused) {
        toggleFocus(true);
        return;
      }

      shootNextPhoto({ resetFocusAfterShot: true });
    });

    touchQuery.addEventListener("change", updateInteractionLabel);
    updateInteractionLabel();
  });
</script>
